- if user != current_user
  %tr
    %td= link_to user.name, user
    %td= user.email
    %td= user.posts.count
    %td= link_to "Edit", edit_user_path(user), class: 'btn btn-highlight'
    %td= link_to "Delete", user, method: :delete, data: { confirm: "Are your sure?" }, class: 'btn btn-danger'